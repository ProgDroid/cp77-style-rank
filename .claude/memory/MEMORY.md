# Project memory — Style Rank

Memories live beside the repo so they survive a move and reach cloud sessions.
One file per fact; this index is loaded each session.

- [Line-ending gate recipe](line-ending-gate-recipe.md) — `--check` failing on Windows is CRLF, not drift; never fix it with `git add --renormalize`.
- [Vendored patch discarded](vendored-patch-discarded-2026-09-22.md) — the old local GameUI.lua fix is upstream now; do not resurrect it.
